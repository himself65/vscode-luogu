"use strict";

import * as vscode from 'vscode';

export async function submitSolution(channel: vscode.OutputChannel, uri?: vscode.Uri): Promise<void> {
    // let edtior = vscode.window.activeTextEditor;
    // let text = edtior.document.getText();
    // TODO
    vscode.window.showInformationMessage('这部分根本没有做完QAQ');
}